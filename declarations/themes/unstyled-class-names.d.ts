import type { ClassNames } from 'choices.js';
/**
 * Minimal classNames: BEM hooks only so Choices JS keeps working.
 * App supplies all visual utilities via `@config.classNames` / CSS.
 */
export declare const UNSTYLED_CLASS_NAMES: ClassNames;
//# sourceMappingURL=unstyled-class-names.d.ts.map